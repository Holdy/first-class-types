{
  "name": "first-class-types",
  "version": "0.0.1",
  "description": "Because it's time that types were more than 'string' and 'number'.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "build": "tcs"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  },
  "license": "MIT"
}
